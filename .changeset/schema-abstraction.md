---
"@achmadya-dev/mcp-core": minor
---

Add schema abstraction (`schema`, `safeParse`) so consumers no longer import Zod directly. `defineTool` returns `RegisterableTool` for portable declaration emit.
